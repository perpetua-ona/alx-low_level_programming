#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */

int main(void)

{
int numbers;

for (numbers = 0 ; numbers < 10; numbers++)
{
printf("%numbers", numbers);
}

printf("\n");

return (0);

}
